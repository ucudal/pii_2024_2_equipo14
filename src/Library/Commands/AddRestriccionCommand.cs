using Discord;
using Discord.Commands;
using Discord.WebSocket;
/*
namespace Library;

public class AddRestriccionCommand : ModuleBase<SocketCommandContext>
{
    [Command("restriccion")]
    [Summary("Elige la restriccion que deseas agregar")]

    public async Task ExecuteAsync([Remainder] string? opponentDisplayName = null)
    {
        bool result = false;
        Entrenador entrenador = new Entrenador("opponentDisplayName");
        Pokemon pokemon = new AddPokemonCommand();
        item item = new UseItemCommand();
        result= Facade.Instance.AgregarRestriccion(entrenador, pokemon.Nombre, pokemon.Tipo, item);
    }
}*/